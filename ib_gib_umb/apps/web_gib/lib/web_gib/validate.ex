defmodule WebGib.Validate do
  @moduledoc """
  Validation logic for sanitizing front-facing data.

  IOW, this isn't inside the engine, because it is a wrapper specifically
  geared towards scary input.
  """
end
