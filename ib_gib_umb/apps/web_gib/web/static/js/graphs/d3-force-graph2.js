import { D3ForceGraph } from './d3-force-graph';

export class D3ForceGraph2 extends D3ForceGraph {
  constructor(graphDiv, svgId) {
    super(graphDiv, svgId);
  }


  // getNodeShape(d) { return "circle"; }
}
